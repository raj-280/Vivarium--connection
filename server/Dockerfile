"""
server/Dockerfile  [STUB — production only]

Will build the FastAPI server image for container deployment.
Not needed for local development — run with: uvicorn main:app --reload
"""
# FROM python:3.12-slim
# WORKDIR /app
# COPY requirements.txt .
# RUN pip install --no-cache-dir -r requirements.txt
# COPY . .
# CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
